import { Header } from "./Header/Header";
import { MainContent } from "./MainContent";

export const PageLayout = () => {
  return (
    <div>
      <Header />
      <MainContent />
    </div>
  );
};
